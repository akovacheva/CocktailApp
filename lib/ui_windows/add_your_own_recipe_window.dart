import 'package:flutter/material.dart';

import '../main.dart';

class AddYourOwnRecipeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      appBar: CommonAppBar(),
    );
  }
}
